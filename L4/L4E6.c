#include<stdio.h>
void main(void)
{
	int vendas[4][12],p,m;
	for(p=0;p<=3;p++)
		for(m=0;m<=11;m++)
		{
			printf("Insira a venda do produto %i mes %i: ", p+1, m+1);
			scanf("%i", &vendas[p][m])
		}
	for()
}